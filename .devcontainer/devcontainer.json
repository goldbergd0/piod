// For format details, see https://aka.ms/devcontainer.json. For config options, see the
// README at: https://github.com/devcontainers/templates/tree/main/src/docker-existing-dockerfile
// {
//   "name": "Raspberry Pi Audio Cross-Compile",
//   "image": "debian:bullseye",
//   "features": {
//     "ghcr.io/devcontainers/features/common-utils:2": {},
//     "ghcr.io/devcontainers/features/cmake:1": {},
//     "ghcr.io/devcontainers/features/ninja:1": {},
//     "ghcr.io/devcontainers/features/git:1": {}
//   },
//   "postCreateCommand": "apt-get update && apt-get install -y g++-aarch64-linux-gnu gcc-aarch64-linux-gnu pkg-config",
//   "customizations": {
//     "vscode": {
//       "extensions": [
//         "ms-vscode.cpptools",
//         "ms-vscode.cmake-tools",
//         "twxs.cmake"
//       ]
//     }
//   },
//   "remoteUser": "vscode",
//   "workspaceFolder": "/workspace"
// }

{
	"name": "piod",
	"build": {
		// Sets the run context to one level up instead of the .devcontainer folder.
		"context": "..",
		// Update the 'dockerFile' property if you aren't using the standard 'Dockerfile' filename.
		"dockerfile": "Dockerfile"
	},
	"runArgs": [
		"--memory=4g" // Limit memory to 2GB
		// "--memory-swap=2g" // Limit swap memory to 2GB (optional, defaults to memory limit if not specified)
	],
	"customizations": {
		"vscode": {
		"extensions": [
			"ms-vscode.cmake-tools",
			"ms-vscode.cpptools", // Recommended: C/C++ extension for IntelliSense and debugging
			"-github.copilot",
            "-github.copilot-chat"
		]
		}
	},
	"mounts": [
        "source=${localWorkspaceFolder}/export,target=/app/export,type=bind"
    ]
	

	// Features to add to the dev container. More info: https://containers.dev/features.
	// "features": {},

	// Use 'forwardPorts' to make a list of ports inside the container available locally.
	// "forwardPorts": [],

	// Uncomment the next line to run commands after the container is created.
	// "postCreateCommand": "cat /etc/os-release",

	// Configure tool-specific properties.
	// "customizations": {},

	// Uncomment to connect as an existing user other than the container default. More info: https://aka.ms/dev-containers-non-root.
	// "remoteUser": "devcontainer"
}
